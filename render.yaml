services:
- type: web
  name: système de gestion des archives
  runtime: node
  plan: free
  buildCommand: npm install
  startCommand: npm start
  envVars:
    - key: DATABASE_URL
      fromDatabase:
        name: sgae-db
        property: connectionString

databases:
  - name: sgae-db
    plan: free
